node_modules
.turbo

.env
.dev.vars
.dev.vars.*

# DB dumps often contain OAuth tokens in user rows — never commit
database_dump.sql

# Editor / backup copies of config (often still contain secrets)
*.toml.back
wrangler.toml.back

# Markdowns for AI
GEMINI.md
CLAUDE.md

# Others
EMAIL_SETUP.md

.docs
